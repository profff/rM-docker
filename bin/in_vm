#!/bin/sh

set -eux

# Use sshpass with known password for build-time SSH access
sshpass -p 'remarkable' ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password root@localhost /bin/sh -l -c "'$*'"
